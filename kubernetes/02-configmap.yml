apiVersion: v1
kind: ConfigMap
metadata:
  name: jerney-configmap
  namespace: jerney-ns
  labels:
    app.kubernetes.io/part-of: jerney
data:
  # property-like keys; each key maps to a simple value
  POSTGRES_DB : jerney_db
  DB_HOST : db
  PORT : "5000"
  DB_NAME : jerney_db
  DB_PORT: "5432"